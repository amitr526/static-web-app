{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": { 
    "start": "sirv ./src/frontend public --cors --single --no-clear --port 8000"
  },
  "dependencies": {
    "@azure/functions": "4.7.3-beta.0"
  },
  "devDependencies": {
    "@types/node": "^20.x"
  },
  "main": "src/api/functions/*.js"
}